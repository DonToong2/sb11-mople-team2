spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mople_db
    username: mople
    password: mople1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: email, profile

logging:
  level:
    com.codeit.mople: DEBUG

jwt:
  secret: ${JWT_SECRET:local-dev-secret-key-change-this-1234567890}

cookie:
  secure: false

admin:
  password: "Admin1234!"

external-api:
  tmdb:
    base-url: https://api.themoviedb.org/3
    api-key: ${TMDB_API_KEY:}
  sportsdb:
    base-url: https://www.thesportsdb.com/api/v1/json
    api-key: ${SPORTSDB_API_KEY:}

oauth2:
  redirect:
    success-uri: http://localhost:8080/
    failure-uri: http://localhost:8080/